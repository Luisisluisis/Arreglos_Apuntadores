#include <iostream>

using namespace std;

struct product {
    double price;
    int posicion;
};

int main() {
    int amountProducts;
    cout<<"Digit the amount of products you want on the machine";
    cin >> amountProducts;

    const int columnas= 3;

    product machine[amountProducts][columnas];

    for (int i=0; i <)
    
    machine[0][0] = {"Coca-cola", 1.000, 1}

    for (int i=0; i<5; i++) {
        for (int j = 0; j < 3; j++) {
            cout << calculator[i][j] << "";
        }
    }
    return 0;
}